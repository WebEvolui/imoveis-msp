---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import PropertyCard from '../components/PropertyCard.astro';
import FloatingWhatsApp from '../components/FloatingWhatsApp.astro';
import { properties } from '../data/properties';
import { siteConfig } from '../config/site';

const heroImage = "https://images.unsplash.com/photo-1544984243-ec57ea16fe25?q=80&w=2000";

const schema = {
  "@context": "https://schema.org",
  "@type": "RealEstateAgent",
  "name": siteConfig.name,
  "image": heroImage,
  "telephone": siteConfig.phone,
  "email": siteConfig.email,
  "address": {
    "@type": "PostalAddress",
    "streetAddress": siteConfig.address,
    "addressCountry": "BR"
  },
  "url": Astro.site
};
---

<Layout 
  title={`${siteConfig.name} - Imóveis exclusivos em ${siteConfig.address}`} 
  description="Encontre os melhores imóveis em São Paulo. Casas, apartamentos e studios de alto padrão."
  image={heroImage}
>
  <script type="application/ld+json" slot="head" set:html={JSON.stringify(schema)} />
  <Header />
  
  <main>
    <!-- Hero Section -->
    <section class="relative h-[80vh] min-h-[500px] flex items-center justify-center bg-gray-900 text-white overflow-hidden">
      <!-- Background Image Placeholder - Replace with actual image -->
      <div class="absolute inset-0 z-0">
        <img 
          src="https://images.unsplash.com/photo-1544984243-ec57ea16fe25?q=80&w=2000" 
          alt="Morro de São Paulo" 
          class="w-full h-full object-cover opacity-60"
        />
      </div>

      <div class="relative z-10 text-center px-4 max-w-4xl mx-auto">
        <h1 class="text-5xl md:text-7xl font-bold mb-4 tracking-tight drop-shadow-md">
          {siteConfig.name}
        </h1>
        <p class="text-xl md:text-2xl mb-2 font-light">
          Seu corretor de confiança: <span class="font-semibold">{siteConfig.brokerName}</span>
        </p>
        <p class="text-sm opacity-80 mb-10">CRECI: {siteConfig.creci}</p>
        
        <a 
          href="#imoveis" 
          class="inline-block bg-primary hover-bg-primary-dark text-white font-bold py-4 px-10 rounded-full text-lg transition-all transform hover:scale-105 shadow-lg border-0 border-transparent hover:border-blue-400"
        >
          Ver Imóveis Disponíveis
        </a>
      </div>
    </section>

    <!-- Properties List -->
    <section id="imoveis" class="py-20 bg-gray-50">
      <div class="container mx-auto px-4">
        <div class="text-center mb-16">
          <h2 class="text-4xl font-bold text-brand-dark mb-4">Imóveis em Destaque</h2>
          <div class="w-24 h-1 bg-primary mx-auto rounded"></div>
          <p class="mt-4 text-gray-600 max-w-2xl mx-auto">
            Confira nossa seleção exclusiva de imóveis para venda e locação.
            Oportunidades únicas escolhidas para você.
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
          {properties.map((property) => (
            <PropertyCard property={property} />
          ))}
        </div>
      </div>
    </section>

    <!-- Why Choose Us -->
    <section id="sobre" class="py-20 bg-white">
      <div class="container mx-auto px-4">
        <div class="text-center mb-16">
          <h2 class="text-4xl font-bold text-brand-dark mb-4">Por que nos escolher?</h2>
          <div class="w-24 h-1 bg-primary mx-auto rounded"></div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
          <!-- Reason 1 -->
          <div class="text-center p-8 rounded-2xl hover:bg-gray-50 transition-colors border border-gray-100 shadow-sm hover:shadow-md">
            <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-primary-light text-primary mb-6">
              <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-800 mb-4">Experiência e Segurança</h3>
            <p class="text-gray-600">
              Anos de mercado garantindo negociações seguras e transparentes. 
              Sua tranquilidade é nossa prioridade.
            </p>
          </div>

          <!-- Reason 2 -->
          <div class="text-center p-8 rounded-2xl hover:bg-gray-50 transition-colors border border-gray-100 shadow-sm hover:shadow-md">
            <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-primary-light text-primary mb-6">
              <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-800 mb-4">Atendimento Ágil</h3>
            <p class="text-gray-600">
              Sabemos que seu tempo é valioso. Oferecemos um atendimento rápido, 
              personalizado e disponível quando você precisa.
            </p>
          </div>

          <!-- Reason 3 -->
          <div class="text-center p-8 rounded-2xl hover:bg-gray-50 transition-colors border border-gray-100 shadow-sm hover:shadow-md">
            <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-primary-light text-primary mb-6">
              <svg xmlns="http://www.w3.org/2000/svg" class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
              </svg>
            </div>
            <h3 class="text-xl font-bold text-gray-800 mb-4">As Melhores Oportunidades</h3>
            <p class="text-gray-600">
              Uma curadoria rigorosa para oferecer apenas imóveis com excelente 
              custo-benefício e potencial de valorização.
            </p>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer />
  <FloatingWhatsApp />
</Layout>
